stages:
- name: comp
  steps:
  - publishImageConfig:
      dockerfilePath: ./Dockerfile
      buildContext: .
      tag: hotsapp/appfront
      pushRemote: true
      registry: regcred
timeout: 60
notification: {}
